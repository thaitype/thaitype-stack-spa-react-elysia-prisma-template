# Goal

Integrate Prismabox to auto-generate TypeBox schemas from Prisma models.
Remove manual domain types — use Prisma-generated types as the single source of truth.
Use TypeBox `t.Pick` / `t.Partial` to derive create/update schemas from the base model.
