# Goal

Refactor DI to use factory container pattern (appContext + createContainer) and add pino structured logging.

Reference: `_ref/thaitype-stack-mongodb-template/src/server/context/app-context.ts`
